# Quality gate: lint, typecheck and unit tests for the frontend; fmt, clippy
# and unit tests for the Rust backend. Runs on every push/PR to main.
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint and format (Biome)
        run: npx biome ci .

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Unit tests
        run: npx vitest run

      # tsc alone doesn't exercise Vite's bundling; until now the first real
      # bundle happened at release time, on a tag.
      - name: Production bundle
        run: npx vite build

  rust:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file \
            libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      # generate_context! embeds frontend assets; give it a stub dist so
      # clippy can compile without running the real frontend build
      - name: Stub frontend dist
        run: mkdir -p dist && echo '<!doctype html>' > dist/index.html

      - name: Format check
        run: cargo fmt --check
        working-directory: src-tauri

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
        working-directory: src-tauri

      - name: Unit tests
        run: cargo test
        working-directory: src-tauri

  # Everything under #[cfg(target_os = "macos")] — the RunEvent::Opened
  # handler, install_cli_command, the macOS menu branches — is invisible to
  # the Linux job. Without this leg those blocks are first compiled on a
  # release tag. No bundle, no signing: clippy + tests only.
  rust-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Stub frontend dist
        run: mkdir -p dist && echo '<!doctype html>' > dist/index.html

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
        working-directory: src-tauri

      - name: Unit tests
        run: cargo test
        working-directory: src-tauri
